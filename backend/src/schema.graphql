# import * from './generated/prisma.graphql'

type Mutation {
  createProduct(
    title: String
    description: String
    price: Int
    image: String
  ): Product!
}

type Query {
  products: [Product]!
}
